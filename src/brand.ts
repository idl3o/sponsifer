/**
 * The product's name, in one place, while the name is under review.
 *
 * Only prose a person reads goes through here. Identifiers keep their spelling
 * until a rename is decided: the localStorage key, the package, the command.
 * docs/rename.md lists each one.
 */

/** The name shown to people. */
export const PRODUCT = 'Sponsifer';

/** The line under the name in the header. */
export const TAGLINE = 'price it, prove it, pitch it';
