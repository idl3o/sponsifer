"""The product's name, in one place, while the name is under review.

Only prose a person reads goes through here. Identifiers that other files and
other people's tools depend on keep their spelling until a rename is decided:
the command, the package, the SSHSIG namespace, the C2PA assertion label and
the home directory. docs/rename.md lists each one.
"""

from __future__ import annotations

#: The name shown to people.
PRODUCT = "Sponsifer"
